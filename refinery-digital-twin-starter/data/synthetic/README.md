# Synthetic Data
All data here is fictional and exists only to demonstrate the architecture. It must not be represented as actual plant data.

Runtime should ultimately read normalized output only:
`data/synthetic -> pipeline -> data/normalized -> app`

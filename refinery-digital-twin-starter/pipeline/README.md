# Pipeline
Expected first modules: `normalize_assets.py`, `normalize_connections.py`, `normalize_telemetry.py`, `normalize_process_paths.py`, `normalize_scenarios.py`, `validate_normalized.py`.
The runtime consumes only validated normalized output.

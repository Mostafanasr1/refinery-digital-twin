Stage C Task 2, vertical slice. One frame, CAM-6, photoreal day. Push that single frame to the limit of the current pipeline. Judged at CAM-6 only.

In scope: dense non-canonical dressing (pipe racks, cable trays, stair towers, secondary vessels, lighting masts, valves and flanges on visible pipe runs, ground clutter and vehicles) as a switchable, non-selectable group; ground detail with wear, tyre tracks and tonal variation near the plant; builder detail raised on every type visible at CAM-6; texture wear and grime tuned per material; lighting and post tuned for that frame.

Rules: dressing stays non-canonical and grouped so it can be switched off. Canonical data unchanged. Engineering regression unchanged. CC0 or procedural only. Budgets for this task: photoreal frame time p95 at or below 25 ms at CAM-6, lazy assets at or below 60 MB.

Deliver: CAM-6 photoreal before and after, side by side with docs/reference/petromind-wide.png; the six-camera set in both looks; metrics; a note on which change gave the most visual gain per millisecond. No claim of reference-level realism; the reviewers judge. Stop at the slice gate.
